#!/usr/bin/env bash
# Mock systemctl — configurable via MOCK_SYSTEMCTL_EXIT and MOCK_SYSTEMCTL_OUTPUT
exit_code="${MOCK_SYSTEMCTL_EXIT:-0}"
output="${MOCK_SYSTEMCTL_OUTPUT:-}"
[[ -n "${output}" ]] && echo "${output}"
exit "${exit_code}"
